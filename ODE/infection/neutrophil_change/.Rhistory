getwd()
setwd('minor stage/brandwonden_git/Brandwonden-Project-IAS/Previous Work Code/HIIS/infection/neutrophil_change/')
filelist = list.files(pattern = ".*.txt")
#assuming tab separated values with a header
datalist = lapply(filelist, function(x)read.table(x, header=T))
filelist = list.files(pattern = ".*.txt")
#assuming tab separated values with a header
datalist = lapply(filelist, function(x)read.table(x, header=T))
print(filelist)
#assuming tab separated values with a header
datalist = lapply(filelist, function(x)read.table(x,sep ='/t', header=T))
#assuming tab separated values with a header
datalist = lapply(filelist, function(x)read.table(x,sep ='\t', header=T))
header(datalist)
head(datalist)
row.names(datalist)
row.names(datalist[1])
head(datalist[1])
datafr = do.call("rbind", datalist)
datafr = do.call("cbind", datalist)
head(datafr)
head(datafr[1:])
head(datafr[1,])
#assuming tab separated values with a header
datalist = lapply(filelist, function(x)read.table(x, header=T))
